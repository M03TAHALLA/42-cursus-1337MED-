#include "libft.h" // Assurez-vous que ce fichier d'en-tête contient les prototypes des fonctions que vous utilisez.
#include <stdio.h>
int main() {
    char *number = ft_itoa(-25);
    printf("%s\n",number);

    return 0;
}
